import React from 'react'
import NavButton from '../navbarbutton'

const Nav = () => {
  return (
    <div>

        <NavButton />
        <NavButton />
        <NavButton />
        <NavButton />

    </div>
  )
}

export default Nav